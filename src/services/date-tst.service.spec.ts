/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {DateTST} from './date-tst.service';

describe('DateTST', () => {
  it('should create an instance', () => {
    expect(new DateTST()).toBeTruthy();
  });
});